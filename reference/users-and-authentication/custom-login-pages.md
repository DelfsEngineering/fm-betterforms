# Custom Login Pages

This page is now a legacy pointer.

The canonical page for this topic is:

- [Custom Login Pages](../authentication/custom-login-pages.md)

Use the canonical page for:

- current auth-page slug guidance
- action mapping for login, registration, reset, verification, and magic-link flows
- reference-style notes about `isForm`, autofill, autofocus, and `model.authMessage`

## Legacy Note

This older page is being retained as a breadcrumb so existing links still land somewhere useful while authentication docs are consolidated into `reference/authentication/`.

